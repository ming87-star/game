# 그림 만들기 — AI에게 시킬 때의 규격

지금 모든 그림은 코드가 도형으로 그립니다 (`js/textures.js`). 진짜 그림이
들어오면 그 파일을 걷어내고 `load.image()`로 바꿉니다. **그러려면 크기·방향·
여백이 지금과 맞아야 합니다.** 이 문서는 그 규격입니다.

---

## 0. 먼저 이것만은

| 규칙 | 왜 |
|---|---|
| **PNG · 배경 투명** | 탑 안은 어두운 남색입니다. 흰 배경이 붙어 오면 네모가 둥둥 떠다닙니다 |
| **아래 표의 4배 크기로** | 32px를 AI에게 직접 시키면 뭉갭니다. 크게 그려서 줄입니다 |
| **오른쪽을 봅니다** | 코드가 왼쪽으로 갈 때 좌우를 뒤집습니다. 왼쪽을 보는 그림을 주면 전부 거꾸로 갑니다 |
| **그림자·바닥·테두리 없이** | 게임이 알아서 얹습니다. 그려 넣으면 발판 위에 검은 얼룩이 생깁니다 |
| **한 마리씩 따로** | 시트로 붙여 오면 잘라야 하고, 잘린 자리가 어긋납니다 |
| **글자 금지** | 어떤 그림에도 문자를 넣지 마세요 |

한 장만 먼저 만들어 보내 주세요. **기는 것(`e-crawler`) 한 장**이면 스타일이
맞는지 바로 판단할 수 있습니다. 스타일이 정해지면 나머지를 쭉 뽑으면 됩니다.

---

## 1. 스타일 — 이 문장을 모든 그림에 똑같이 붙이세요

프롬프트 뒤에 **글자 하나 바꾸지 말고** 붙입니다. 이게 서른 장을 한 세계처럼
보이게 하는 유일한 장치입니다. 그림마다 스타일 문장을 조금씩 고치면
세계가 아니라 그림 모음이 됩니다.

```
2D game sprite, side view, facing right, chunky readable silhouette,
bold clean outlines, flat cel shading with two tone shadows,
light source from upper left, saturated colors, dark fantasy but friendly,
mobile game art, centered single subject, full body visible,
transparent background, no ground shadow, no text, no border, no frame
```

**금지어(negative prompt):**

```
photorealistic, 3d render, blurry, soft focus, gradient background,
white background, drop shadow, ground shadow, text, watermark, signature,
multiple subjects, cropped, cut off, motion blur
```

### 배경색을 알려 주세요

탑 안쪽 벽은 `#1D2542`, 그 바깥은 `#141A2E`입니다. **어두운 남색 위에 놓입니다.**
그래서 주인공도 적도 **남색·검정 계열이면 안 됩니다.** 배경에 묻힙니다.
색이 어두워야 하는 놈(유령·그림자)은 **밝은 테두리**를 넣어 주세요.

---

## 2. 사람 — 주인공 셋

바탕 크기 아래와 같고, **4배로 그려서 주세요** (괄호 안).
발이 그림 맨 아래에 닿아야 합니다. 위쪽에 빈칸이 남는 것은 괜찮습니다.

| 파일 | 크기 | 4배 | 누구 |
|---|---|---|---|
| `player-warrior.png` | 38×48 | **152×192** | 전사 |
| `player-archer.png` | 42×48 | **168×192** | 궁수 |
| `player-rogue.png` | 40×48 | **160×192** | 도적 |

셋은 **실루엣에서 갈려야 합니다.** 색만 다르면 작게 줄였을 때 똑같아 보입니다.

- **전사** — 각진 어깨, 뿔이나 깃이 달린 투구, 셋 중 가장 넓음. 붉은 기(`#EF9A9A`)
- **궁수** — 뾰족한 후드, 등에 멘 활, 몸이 가늘고 세로로 김. 초록 기(`#A5D6A7`)
- **도적** — 낮게 웅크린 자세, 뒤로 날리는 망토, 얼굴 가리개. 보라 기(`#CE93D8`)

> 무기는 손에 들려 있어도 되지만 **작게** 그려 주세요. 무기는 따로 그림이 있고,
> 판이 진행되면 열두 자루가 바뀝니다. 몸에 붙은 무기가 크면 안 맞습니다.

### 이 한 장은 **네 조각으로 잘려서** 움직입니다

공격 모션이 주인공을 조각내어 따로 움직입니다 (`js/motion.js`, `art/p-*.svg`).
망토 · 다리 · 몸통 · 무기 쥔 팔 넷이고, 조각마다 축이 있습니다. 자르는 것은
**코드 쪽에서 이미 해 두었습니다** — 원본 그림에서 칠도 좌표도 안 바꾸고
떼어 온 것이라, 쉬는 자세에서는 원래 한 장이던 모습 그대로 겹칩니다.

그래서 이 한 장을 그리거나 고칠 때 지켜야 할 것:

- **덩어리마다 주석으로 이름을 붙여 주세요** (`<!-- 망토 -->` `<!-- 다리 -->`
  `<!-- 몸통 -->` `<!-- 무기 쥔 팔 -->`). 자르는 사람이 어디서 끊을지를
  이 주석으로 찾습니다. 지금 셋이 다 그렇게 되어 있습니다.
- **무기와 그 무기를 쥔 팔은 한 덩어리로.** 팔이 몸통 path 와 섞여 있으면
  잘라 낼 수가 없습니다.
- **서 있는 자세는 가운데를 지켜 주세요.** 앞뒤 어느 쪽으로도 기울지 않은
  자세라야, 허리를 앞으로 돌릴 때와 뒤로 누일 때가 둘 다 자연스럽습니다.
- **발이 그림 맨 아래에 닿아야 합니다.** 다리는 거의 안 움직이는 조각이라,
  발이 떠 있으면 몸만 움직이고 발은 허공에 남습니다.
- **관절이 될 자리는 둥글게.** 팔이 시작하는 곳은 `stroke-linecap="round"` 인
  굵은 선이면 그 자체가 둥근 관절이 되어, 크게 돌려도 이음매가 안 보입니다.
- **어깨(견갑)는 몸통에 남깁니다.** 팔이 그 아래에서 도니까 어깨를 크게 돌려도
  갑옷이 관절을 덮어 줍니다.

궁수의 활은 **등이 아니라 앞손에** 있어야 합니다. 등에 메면 쏘는 동안 앞손은
빈손으로 허공을 잡고 활은 등에서 흔들립니다 — 당기는 것도 놓는 것도 안 보입니다.

나중에 **손에 든 무기를 코드가 갈아 끼우게** 하려면, 무기를 뺀 맨몸 한 벌
(`player-*-bare.png`)을 같은 규격으로 하나 더 떠 주시면 됩니다. 그때는 서른여섯
자루가 손에서 직접 바뀝니다.

프롬프트 예:

```
A warrior hero for a tower climbing game, side view facing right,
broad angular pauldrons, horned helmet with a red crest, sturdy stance,
red and steel gray armor,
<스타일 문장>
```

---

## 3. 적 — 열두 종류

**전부 오른쪽을 봅니다.** 게임 안에서 종류마다 크기를 다르게 늘리므로
(거인은 1.9배) 그림 자체는 아래 크기 기준으로 그리면 됩니다.

| 파일 | 크기 | 4배 | 이름 | 어떻게 움직이나 | 색 |
|---|---|---|---|---|---|
| `e-crawler.png` | 32×32 | 128×128 | 기는 것 | 땅을 기어 다가옴 | 붉음 `#EF5350` |
| `e-hopper.png` | 34×32 | 136×128 | 뛰는 것 | 개구리처럼 뜀 | 연두 `#8BC34A` |
| `e-flyer.png` | 36×32 | 144×128 | 날것 | 좌우로 흔들며 날아옴 | 보라 `#7E57C2` |
| `e-brute.png` | 32×34 | 128×136 | 단단한 놈 | 느리고 두꺼움 | 갈색 `#8D6E63` |
| `e-charger.png` | 36×38 | 144×152 | 돌진병 | 멈춰 노려보다 가로로 돌진 | 방패 든 갈색 `#795548` |
| `e-dasher.png` | 32×30 | 128×120 | 빠른 놈 | 아주 빠름, 앞으로 쏠린 모양 | 노랑 `#FFCA28` |
| `e-bomber.png` | 34×36 | 136×144 | 폭탄충 | 날아와 **죽으면서 터짐** | 회청 + 불붙은 심지 `#546E7A` |
| `e-giant.png` | 36×38 | 144×152 | 거인 | 크고 느림 (1.9배로 커집니다) | 자주 `#AD1457` |
| `e-splitter.png` | 36×36 | 144×144 | 쪼개지는 것 | **죽으면 둘로 갈라짐** — 가운데 금 | 청록 `#00897B` |
| `e-shooter.png` | 34×34 | 136×136 | 사수 | 거리를 두고 쏨. 눈 하나 + 포신 | 청록 `#26A69A` |
| `e-diver.png` | 36×38 | 144×152 | 급강하 | 머리 위로 올라가 내리꽂음 | 남보라 + 노란 부리 `#5C6BC0` |
| `e-ghost.png` | 34×36 | 136×144 | 유령 | 나타났다 사라졌다 | 연보라 반투명 `#B39DDB` |

**중요한 것 세 가지**

1. **뭐가 위험한지가 실루엣에 보여야 합니다.** 돌진병은 방패, 폭탄충은 심지,
   쪼개지는 것은 가운데 금, 급강하는 뾰족한 부리.
2. **눈은 크고 밝게.** 32px로 줄면 눈만 남습니다. 그게 "살아 있는 것"으로 읽히는
   유일한 신호입니다.
3. **색은 위 표를 지켜 주세요.** 플레이어가 색으로 종류를 외웁니다. 지금 색을
   그대로 쓰면 지금까지의 감각이 그대로 넘어옵니다.

---

## 4. 박쥐 둘

상점을 오래 안 들르면 몰려와 코인과 아이템을 채 갑니다.

| 파일 | 크기 | 4배 | 무엇 |
|---|---|---|---|
| `bat-thief.png` | 40×32 | 160×128 | 도둑 — 보라 `#7E57C2`, **자루를 들고 있음** |
| `bat-biter.png` | 40×32 | 160×128 | 무는 놈 — 붉음 `#C62828`, **송곳니가 보임** |

둘은 **한눈에 갈려야 합니다.** 무는 놈이 오면 피해야 하고 도둑은 코인만
뺏기기 때문입니다. 색과 함께 자루/송곳니가 실루엣에 나와야 합니다.

---

## 5. 보스 다섯 — 랜덤으로 하나가 나옵니다

한 놈에 패턴 넷을 물려 두었던 것을, **놈마다 패턴이 다른 다섯**으로 나눠
그렸습니다. 보스 층에 들어설 때 하나가 뽑힙니다.

| 보스 | 그림 | 탄 | 색으로 갈리는 것 | 어떻게 싸우게 그렸나 |
|---|---|---|---|---|
| 탑의 수문장 | `boss-warden.png` | `boss-shot.png` | 붉은 두 눈 `#FF5252` | 줄을 골라 내리꽂기 (지금 것 — volley · sweep · slam) |
| 외눈 | `boss-gazer.png` | `boss-shot-gazer.png` | 청록 외눈 `#26C6DA` | 한 줄을 오래 노려보다 굵은 빔 |
| 집게 | `boss-crusher.png` | `boss-shot-crusher.png` | 달군 주황 날 `#FF7043` | 바깥 두 줄을 찍고 가운데를 덮침 |
| 알품은 것 | `boss-brood.png` | `boss-shot-brood.png` | 연두 알주머니 `#9CCC65` | 졸개를 계속 뱉음 (탄이 깨지면 나옴) |
| 갈라진 가면 | `boss-phantom.png` | `boss-shot-phantom.png` | 창백한 흰보라 `#E1BEE7` | 사라졌다 다른 자리에 나타남 |

전부 `320×240` (`data-bake-scale="2"` 로 640×480 에 구움), 탄은 `36×36` 4배입니다.

### 다섯을 한 핏줄로 묶는 것

- 남보라 몸 `#311B92` / `#4527A0`
- 위로 벌어진 **왕관 뿔**
- 발판 가까이 내려오는 **아래턱**. 근접이 닿는 자리라 여기가 두툼해야 합니다

### 다섯을 갈리게 하는 것 — **무엇이 위험한가**, 오직 그것뿐

색을 하나씩 나눠 가졌습니다. 붉은 눈이 보이면 내리꽂기, 청록이면 빔,
주황이면 내리찍기, 연두면 졸개, 흰보라면 순간이동. **그 색이 보이는 순간
무엇을 피해야 하는지가 정해져야 합니다.** 그래서 각 색은 그 보스의
위험한 부위에만 씁니다 — 몸통에 칠하면 신호가 아니라 무늬가 됩니다.

`boss-phantom` 만 외곽선이 어둡지 않고 밝습니다. 몸이 반투명해 어두운 벽에
묻히기 때문입니다 (§1 의 "색이 어두워야 하는 놈은 밝은 테두리" 규칙).

### 탄은 위아래 방향입니다

다른 그림과 달리 옆을 보지 않습니다. 위에서 아래로 떨어지므로 **떨어지는
쪽(아래)이 뾰족해야** 어디로 오는지가 읽힙니다.

### 그리면서 걸린 것

| 증상 | 원인 | 고친 법 |
|---|---|---|
| 집게가 **주황 줄 그은 납작한 날개**로 보임 | 판 하나에 무늬만 얹음 | 두 갈래를 마주 보게 벌리고 사이를 비움. 달군 날은 **맞물리는 안쪽 면에만** — 바깥에 넣으면 무늬가 되고 안쪽에 넣어야 날이 됨 |
| 집게가 나뭇가지 끝에 달린 것 같음 | 팔이 가늚 | 팔을 몸통 높이만큼 두껍게 |
| 외눈이 **빔 쏘는 놈으로 안 보임** | 빔 예고를 턱 **뒤에** 그려 통째로 가려짐 | 무엇보다 위에 그림. 예고는 가려지면 없는 것과 같음 |

---

## 6. 날아가는 것과 이펙트

여기는 **회전해서 씁니다.** 전부 **오른쪽을 향한 채** 그려 주세요.

| 파일 | 크기 | 4배 | 무엇 | 주의 |
|---|---|---|---|---|
| `arrow.png` | 38×13 | 152×52 | 화살 | 촉이 **오른쪽 끝**, 깃이 왼쪽 끝. 가로로 눕힌 모양 |
| `arrow-trail.png` | 18×3 | 72×12 | 화살 자국 | 그냥 흐릿한 가로 선 |
| `wave.png` | 44×44 | 176×176 | 파동검의 파동 | 초승달이 **오른쪽으로 볼록** |
| `slash.png` | 140×140 | 560×560 | 칼자국 | 눈썹 모양. 가운데 두껍고 양 끝 뾰족 |
| `bullet.png` | 12×12 | 48×48 | 내 탄 | 작은 밝은 덩어리 |
| `enemy-bullet.png` | 16×16 | 64×64 | 적의 탄 | 붉게 — 내 것과 확실히 갈리게 |
| `spark.png` | 10×10 | 40×40 | 불꽃 조각 | 흰 점 하나. 코드가 색을 입힙니다 |
| `coin.png` | 18×18 | 72×72 | 코인 | 금색, 옆에서 본 동전 |

> **`slash` · `wave` · `spark` · `bullet`은 흰색(또는 아주 밝은 회색)으로만
> 그려 주세요.** 코드가 무기 색을 입혀서 씁니다. 색이 들어 있으면 그 색과
> 섞여서 탁해집니다.

---

## 7. 무기 그림 서른여섯 자루 (지금은 안 하셔도 됩니다)

지금은 코드가 값으로 지어냅니다 (흰 외곽선). 나중에 희귀도를 색으로 나눌
계획이라 **이건 마지막에 손대는 것이 좋습니다.** 하실 거라면:

| 파일 | 크기 | 4배 |
|---|---|---|
| `w-warrior-0.png` … `w-warrior-11.png` | 48×48 | 192×192 |
| `w-archer-0.png` … `w-archer-11.png` | 48×48 | 192×192 |
| `w-rogue-0.png` … `w-rogue-11.png` | 48×48 | 192×192 |

- **칼끝이 위를 봅니다** (활은 오른쪽을 봅니다).
- 이름과 순서는 `js/classes.js`의 `weapons` 표를 보세요.
- 단계가 오를수록 **커지고 화려해져야** 합니다 — 열두 번째가 첫 번째와 한눈에
  갈리지 않으면 UP을 밟는 보람이 없습니다.

---

## 7.5 발판 위 아이템 — 그렸고, 게임에 붙었습니다

예전에는 색 동그라미 안에 글자였습니다 (`+1` `속` `방` `＋`). 아이템을 대폭
줄여서 하나하나가 귀해졌으니 **무엇이 놓였는지가 두 층 밖에서 읽혀야** 합니다.
글자는 그 거리에서 안 읽힙니다.

전부 **36×36** (4배 = 144×144). 발판 위에 떠서 위아래로 흔들립니다.
동그라미 배경은 코드가 지웠고 이제 그림만 남습니다 — **그림 자체가 실루엣**입니다.
좌우로 안 뒤집히므로 향하는 쪽은 자유입니다.

| 파일 | 무엇 | 어떻게 |
|---|---|---|
| `item-plus.png` | 공격력 | **빛나는 모루와 강화망치.** 모루 위에 망치가 떠 있고 접점에 불티. 금빛 `#FFD54F` |
| `item-haste.png` | 공격 속도 | **깃털** (또는 가벼운 장갑). 하늘빛 `#4FC3F7`, 끝이 흩날리게 |
| `item-double.png` | ×2 속도 | 같은 깃털인데 **둘**이고 더 밝게. 청록 `#00E5FF` — 가장 귀합니다 |
| `item-armor-warrior.png` | 방어구 (전사) | **견갑/방패 조각.** 강철 `#A8BCCD` + 금테 `#FFC94D` |
| `item-armor-archer.png` | 방어구 (궁수) | 같은 모양, **가죽 초록** `#81C784` + 갈색 끈 |
| `item-dodge.png` | 회피 (도적) | **가벼운 신 + 잔상.** 보라 `#CE93D8` |
| `item-heal.png` | 회복 | **붉은 약병.** 병은 붉게, 빛무리를 초록 `#66BB6A` 으로 |
| `item-medal.png` | 메달 | **훈장.** 금빛에 리본 |

방어구를 직업마다 나누는 이유: 같은 `방`인데 전사와 궁수가 같은 그림이면
"내가 입는 것"이라는 느낌이 없습니다. **모양은 같고 칠만 다릅니다.**

### 함정은 **생김새로도** 갈려야 합니다

지금은 두 층 안에 들어야 홀로그램처럼 깜빡여 정체가 드러납니다. 거기에
생김새의 표를 하나 더 얹습니다 — 알아보는 사람이 이득을 보는 장치입니다.

| 파일 | 무엇 | 진짜와 어떻게 다른가 |
|---|---|---|
| `item-bomb.png` | 폭탄 | **진짜 폭탄.** 검은 쇠공 + 타들어 가는 심지 + 불티. 숨기지 않습니다 |
| `item-fake-plus.png` | 가짜 공격력 | 모루에 **금이 가 있고** 망치가 **부러졌습니다.** 불티 대신 검은 연기 |
| `item-fake-haste.png` | 가짜 속도 | 깃털이 **꺾였고** 끝이 타 있습니다 |
| `item-fake-armor.png` | 가짜 방어구 | 방패에 **구멍**이 뚫려 있습니다 |
| `item-fake-heal.png` | 가짜 회복 | 약병이 **깨졌고** 내용물이 검습니다 |

**같은 물건의 망가진 판**이라는 것이 규칙입니다. 아주 다르게 그리면 멀리서도
바로 들켜 함정이 뜻을 잃고, 똑같이 그리면 알아볼 길이 없어 그냥 세금이 됩니다.
**실루엣은 같고 안쪽이 망가진** 정도가 맞습니다.

### 먹었을 때의 모션

공격력을 먹으면 **망치가 한 번 내리쳐지고 불티가 튀게** 할 생각입니다 (0.4초).
움직이는 것은 코드가 만들지만, 그러려면 **모루와 망치가 따로** 와야 합니다.

| 파일 | 무엇 |
|---|---|
| `item-plus-anvil.png` | 모루만 · 36×28 |
| `item-plus-hammer.png` | 망치만 · 28×28 (자루가 오른쪽 아래로) |

한 장으로 주시면 통째로 흔드는 정도밖에 못 합니다.

### 규격에 없었지만 코드가 요구해서 함께 그린 둘

| 파일 | 왜 |
|---|---|
| `item-fake-dodge.png` | `tower.js` 의 `MIMIC_DISGUISES` 에 `DODGE` 가 들어 있습니다. 가짜가 회피로 둔갑할 수 있는데 그 그림만 없으면 그 경우에만 표가 비어 버립니다 |
| `item-relic.png` | `tower.js:298` 이 아직 `SLOT.RELIC` 을 놓습니다. 나머지가 전부 그림이 되는데 이것만 글자 배지로 남으면 그 층만 이질적으로 보입니다 |

### 그리면서 정한 것들

**방어구는 견갑이 아니라 방패입니다.** 처음에 둥근 판 셋을 겹친 견갑으로
그렸더니 36px 에서 **헬멧으로 읽혔습니다.** 규격이 "견갑/방패 조각"이므로
방패로 갔습니다 — 위가 넓고 아래가 뾰족한 실루엣은 이 아이템 하나뿐이라
멀리서도 안 헷갈립니다. 전사와 궁수는 **경로가 한 글자도 다르지 않고**
칠만 다릅니다.

**가짜의 구멍은 비웁니다.** `item-fake-armor` 의 구멍은 안을 검게 칠하지 않고
mask 로 뚫었습니다. 칠하면 무늬로 보이고, 뚫어야 뒤의 벽이 비쳐 뚫린 것으로
보입니다.

**불티냐 연기냐가 진짜와 가짜를 가릅니다.** `item-plus` 는 망치와 모루가 닿는
자리에서 금빛 불티가 튀고, `item-fake-plus` 는 같은 자리에서 검은 연기가 납니다.
모루와 망치의 각도는 그대로 두었으므로, 멀리서는 같아 보이고 가까이서 갈립니다.

### 걸려 있던 것 — 가죽 가짜를 하나 더 떴습니다

`item-fake-armor` 가 **강철 한 벌뿐**이라, 가죽(궁수) 방패로 둔갑한 가짜가
드러날 때 색까지 바뀌었습니다. 게임에 붙이면서 `item-fake-armor-archer` 를
같은 경로로 하나 더 떴습니다 (경로는 한 글자도 안 다르고 칠만 가죽입니다).

색이 바뀌면 "이건 망가진 그것이었구나"가 아니라 **"다른 물건으로 바뀌었네"**가
됩니다. 함정이 가르치는 것은 같은 물건의 망가진 데를 알아보는 눈이므로,
드러나는 순간에 실루엣만이 아니라 **칠까지 그대로**여야 합니다.

### 게임에 붙었습니다

`SLOT_MARK` 의 `art` · `slotArtKey`(직업 색) · `fakeArtKey`(드러났을 때 갈아
끼울 것) 셋으로 이어집니다. 동그라미와 글자는 **그림이 없을 때만** 나옵니다 —
한 장을 지우면 그 칸만 글자로 돌아가고 나머지는 그대로 돕니다.
확인은 `node verify-art.js` 와 `node shot-items-ingame.js`.

## 7.9 오프닝 네 컷 만화 — **한 장**만 그리면 됩니다

게임을 처음 켠 사람에게 딱 한 번 나옵니다. 그 뒤로는 시작 화면의
「이야기 다시 보기」로만 나옵니다.

### 규격

| | |
|---|---|
| 장수 | **한 장.** 2×2로 네 컷을 그립니다 |
| 비율 | **정사각형** (1:1). 컷 하나도 정사각형이 됩니다 |
| 크기 | 가로 1024px 권장 (컷 하나가 512px) |
| 파일 | `art/story.webp` — jpg·png도 받지만 webp가 가장 가볍습니다 |
| 용량 | **400KB 아래.** 넘으면 `node bake-story.js`가 경고합니다 |
| 자르는 선 | 정확히 한가운데. 코드가 절반씩 잘라 씁니다 |

### 읽는 순서

```
 ┌─────────┬─────────┐
 │  1컷    │   2컷   │
 │ 탑이    │ 올라간  │
 │ 솟은 날 │ 사람들  │
 ├─────────┼─────────┤
 │  3컷    │   4컷   │
 │ 아무도  │ 그리고  │
 │ 안 돌아옴│  당신   │
 └─────────┴─────────┘
```

### 이 그림만은 규칙이 다릅니다

위의 스타일 문장(1절)을 **그대로 쓰지 마세요.** 저것은 발판 위에 놓일
스프라이트용이라 "투명 배경·단일 대상·전신"을 요구하는데, 만화 컷은
정반대입니다 — 배경이 있어야 하고, 여러 사람이 나오고, 화면이 잘립니다.

대신 이 셋만 지켜 주세요.

1. **컷마다 따로 읽혀야 합니다.** 한 컷씩 넘겨 보게 되어 있어서, 네 컷을
   한눈에 보는 사람은 없습니다. 컷 하나만 봐도 무슨 일인지 알아야 합니다.
2. **칸 사이에 여백(거터)을 두지 마세요.** 코드가 정확히 절반에서 자릅니다.
   여백이 있으면 잘린 자리에 흰 띠가 남습니다. 테두리도 넣지 마세요.
3. **글자를 넣지 마세요.** 대사와 설명은 컷 아래에 게임이 직접 적습니다
   (`CFG.story.panels`). 그림 안에 글자가 있으면 두 번 읽게 됩니다.

색은 게임과 같은 어두운 남색 계열(`#141A2E`·`#1D2542`)에 얹히므로,
너무 밝은 흰 배경이면 화면에서 튑니다. 탑은 위압적으로, 사람은 작게.

### 이야기 (지금 들어가 있는 글)

주인공은 **탑 바닥에서 기억 없이 깨어난 사람**입니다. 꼭대기에 이 세계를 지은
자가 있고, 그만이 자기가 누구인지 압니다. 떨어져도 같은 바닥에서 다시
눈을 뜹니다 — 그래서 몇 번이고 오릅니다.

| 컷 | 그림 | 글 |
|---|---|---|
| 1 (왼위) | 어둠 속 차가운 돌바닥, 갑옷 입은 사람이 눈을 뜸. 주변에 아무도 없음 | 차가운 돌바닥에서 눈을 떴습니다. / 이름도, 어떻게 왔는지도 몰랐습니다. |
| 2 (오른위) | 올려다본 시점. 탑 안쪽이 끝없이 위로 이어짐. 내려가는 계단은 없음 | 머리 위로 탑이 끝없이 이어져 있었습니다. / 내려가는 길은 없었습니다. |
| 3 (왼아래) | 벽에 새겨진 오래된 글자·부조. 꼭대기의 형상을 어렴풋이 암시 | 꼭대기에 이 세계를 지은 자가 있다고 했습니다. / 그라면 내가 누구인지 알 것입니다. |
| 4 (오른아래) | 뒷모습. 첫 발판에 올라섬. 위로 갈림길 셋이 희미하게 보임 | 떨어져도 그는 이 바닥에서 다시 눈을 뜹니다. / 탑이 놓아주지 않으니까요. |

### 넣는 법

```
art/story.webp 에 두고
node bake-story.js      → js/storydata.js 로 구움
node build.js           → 합친 파일에 반영
node shot-story.js      → 네 컷이 제대로 잘렸는지 눈으로 확인
```

마지막 줄을 꼭 돌려 보세요. **컷이 어긋나도 게임은 아무 일 없이 돌아갑니다** —
그냥 엉뚱한 자리가 보일 뿐이라 오류가 안 납니다.

글(컷마다 두 줄)은 `js/config.js`의 `CFG.story.panels`에서 고칩니다.
그림과 글을 따로 두었으므로, 그림을 새로 그리지 않고 글만 고쳐도 됩니다.

---

## 7.95 탑에서 만나는 사람 둘 — **한 컷씩, 두 장**

직업이 열리는 순간에 한 컷이 나옵니다. 궁수는 한 판에 500층·코인 1000,
도적은 700층·코인 2000을 채우면 열립니다.

**해금은 쓰러졌을 때 일어납니다.** 그러니 이 두 장은 전부 같은 상황입니다 —
**쓰러진 사람을 누군가가 내려다보고 있는 장면.** 그 한 장면이 두 가지를
한꺼번에 설명합니다. 왜 다음 판에 저 사람으로 오를 수 있는지(탑은 한 번에
하나만 올려보내니 다음 차례는 그 사람), 그리고 왜 나는 또 바닥에서
시작하는지(그가 업고 내려왔으니까).

| | |
|---|---|
| 장수 | **두 장.** 컷 하나짜리라 자르지 않습니다 |
| 비율 | **정사각형** (1:1) 권장 — 오프닝 컷과 같은 액자에 들어갑니다 |
| 크기 | 가로 512~1024px · 한 장에 400KB 아래 |
| 파일 | `art/meet-archer.webp` · `art/meet-rogue.webp` |

### 두 장의 내용

**`meet-archer.webp` — 활을 든 사람** (500층)

바닥에 쓰러진 기사. 그 위로 방금 쏜 화살이 적을 꿰뚫고 있거나, 이미 꿰뚫린
적이 쓰러져 있습니다. 활을 든 사람이 시위를 내리며 그를 내려다봅니다.
**얼굴이 보여야 합니다** — 다음 판의 주인공이니까요. 초록 계열
(`#A5D6A7`, 궁수의 색). 가죽 갑옷, 활.

> 「나도 이 바닥에서 눈을 떴어. 다섯 번 올랐고 다섯 번 떨어졌지. 다음엔 내가 갈게.」

**`meet-rogue.webp` — 그림자 속의 사람** (700층)

쓰러진 기사의 짐을 뒤지던 손이 멈춘 순간. 코인이 바닥에 흩어져 있습니다
(700층·코인 2000을 채운 판이라 **많이** 모아 온 사람입니다). 후드를 쓴 사람이
어둠 속에서 이쪽을 봅니다. 보라 계열(`#CE93D8`, 도적의 색). 단검 두 자루.

> 「이만큼 모아 온 사람은 처음 봐. 위에 뭐가 있는지, 나도 슬슬 궁금해졌거든.」

### 이 두 장도 규칙은 오프닝과 같습니다

**글자를 넣지 마세요.** 대사는 게임이 컷 아래에 직접 적습니다
(`CFG.story.meetings`). 테두리도 넣지 마세요 — 게임이 액자를 그립니다.

세 사람의 색이 서로 갈려야 합니다. 전사는 붉은 계열(`#EF9A9A`),
궁수는 초록, 도적은 보라입니다. 직업 고르기 화면에서 쓰는 색과 같습니다.

---

## 8. 배경과 발판 — 그렸습니다

화면의 대부분을 덮는 자리라 분위기가 가장 크게 바뀌는 곳입니다. `art/` 에
그려 두었습니다.

| 파일 | 크기 | 무엇 | 색 |
|---|---|---|---|
| `plat.png` | 140×20 | 보통 발판 | `#5C6BC0` / 윗면 `#9FA8DA` |
| `plat-shop.png` | 460×20 | 상점 발판 — 나뭇결 좌판 | `#FFB74D` / 윗면 `#FFE0B2` |
| `plat-boss.png` | 460×20 | 보스 투기장 — 갈라진 금 | `#6A1B9A` / 윗면 `#CE93D8` |
| `wall.png` | 500×960 (세로로 이어 붙임) | 탑 안쪽 벽 | `#1D2542` |

**발판은 윗면이 가장 밝습니다.** 어디를 딛는지가 곧 규칙이라, 20px 안에서
쓸 수 있는 대비를 전부 거기에 몰아 줬습니다.

**가로로 늘려 쓸 때** — `plat.png` 는 양 끝 **18px** 안에만 마감이 들어 있습니다.
9-slice 로 자를 때 `left = right = 18`, 가운데는 늘려도 표가 나지 않습니다.
지금 게임에 있는 폭은 140 과 460 둘뿐이라 각각 제 크기로 그려 두었으니,
그대로 쓰면 늘릴 일이 없습니다.

### 벽을 붙일 때 — 이건 그냥 갈아 끼우면 안 됩니다

지금 벽은 `scene-game.js` 에서 **화면에 고정된 단색 사각형**입니다
(`setScrollFactor(0)`). 색만 칠해져 있을 때는 고정이 맞았습니다 — 아무리 올라가도
끊길 무늬가 없었으니까요.

그림을 붙이면 얘기가 달라집니다. 고정한 채로 무늬만 넣으면 **탑이 안 움직입니다.**
발판은 흘러 내려가는데 벽은 제자리에 붙어 있어서, 오르는 느낌이 통째로 죽습니다.
`TileSprite` 로 바꾸고 `tilePositionY` 를 카메라 스크롤에 물려야 합니다.

무늬의 세로 주기는 **960 을 나누어떨어지게** 잡아 두었습니다 — 돌 두 켜가 96
(960 ÷ 96 = 10), 벽감은 480 마다 (960 ÷ 480 = 2). 그래서 몇 장을 이어 붙이든
이음매가 생기지 않습니다. `render-art.js` 가 굽고 나서 벽을 두 장 이어 붙인
자리를 `shots/art-scene.png` 한가운데에 놓고 찍으니, 고친 뒤에는 거기를 보세요.

**값을 `#182040` ~ `#26315a` 안에 가둬 두었습니다.** 주인공과 적이 언제나 배경보다
밝게 남아야 합니다. 여기서 대비를 올리면 그림은 예뻐지고 게임은 안 보이게 됩니다.

---

## 8.5 코드 쪽에서 그리는 길 — `art/*.svg`

AI로 그리는 것과 별개로, **SVG로 그려서 굽는 길**이 열려 있습니다.

```
art/e-crawler.svg      ← 그림 원본 (viewBox 가 곧 게임 안 크기)
node render-art.js     ← assets/e-crawler.png 로 4배 크기로 구움
node shot-art-ingame.js ← 실제 게임에 끼워 넣고 지금 것과 나란히 찍음
```

이 환경에는 ImageMagick 도 rsvg 도 없지만, Playwright 로 깔려 있는 Chromium 이
SVG 를 제대로 그립니다. `omitBackground` 로 찍으면 알파가 살아 있는 PNG 가
그대로 나옵니다.

> 새 클론에서는 `npm install` 뒤에 **`CHROME_PATH` 를 넣어 주세요.** 이 환경에
> 깔린 Chromium 은 Playwright 가 스스로 찾는 자리에 없습니다.
>
> ```
> CHROME_PATH=/opt/pw-browsers/chromium node render-art.js
> ```

**작은 것에는 이쪽이 유리합니다.** 크기가 정확하고, 32px로 줄여도 선이 살아
있고, 서른 장의 선 굵기와 팔레트를 코드가 강제해 줍니다. **큰 것(보스·배경)에는
AI 그림이 유리합니다** — 그려 넣은 질감이 그대로 값어치가 됩니다.

### 칠하는 법 — 서른 장이 똑같이 지키는 네 가지

그림마다 다르게 칠하면 세계가 아니라 그림 모음이 됩니다. 실루엣은 놈마다
달라야 하지만, **칠하는 법은 한 장도 예외 없이 같습니다.**

1. **한 덩어리마다 값을 세 단으로 끊습니다** — 어두운 면(오른쪽 아래) · 바탕 ·
   밝은 면(왼쪽 위). 두 단이면 스티커처럼 납작하고, 그러데이션을 쓰면 셀
   셰이딩이 무너집니다.
2. **림라이트는 왼쪽 위 모서리에만** 얇게 (`stroke="#ffffff" stroke-opacity=".3"`).
   빙 두르면 빛이 아니라 테두리가 됩니다.
3. **눈에는 빛무리를** (`radialGradient`, 눈 지름의 1.5배). 32px로 줄면 눈만
   남으므로, 여기가 켜져 있어야 "살아 있는 것"으로 읽힙니다.
4. **밝은 점 한 쌍은 눈에만 허락합니다.**

### 그리면서 깨진 것들 — 같은 함정에 두 번 빠지지 않도록

| 증상 | 원인 | 고친 법 |
|---|---|---|
| 단단한 놈의 **머리가 둘로** 보임 | 어깨에 박은 밝은 대갈못이 눈으로 읽힘 | 못은 어두운 쇠빛으로. 밝은 점 한 쌍은 눈에만 |
| 단단한 놈이 **나무통**으로 보임 | 좌우 대칭 + 가슴을 가로지르는 띠(=통테) | 등을 왼쪽 위로 부풀리고 머리를 오른쪽으로 내밀어 대각선. 가슴은 V 자 판 |
| 팔·몸·어깨가 **한 덩어리로 뭉침** | 전부 같은 갈색 | 어깨는 밝은 돌빛, 몸통은 중간, 머리는 어둡게 — 세 값을 벌림 |
| 뛰는 것의 뒷다리가 **딴 물체**로 보임 | 다리를 몸 옆에 덩어리로만 붙임 | 허벅지를 몸 안쪽 깊이 밀어 넣고 허벅지→무릎→정강이→발의 Z 를 그림 |
| 입선이 **혀처럼** 달라붙음 | 선이 실루엣 밖으로 나감 | 안쪽에서 끝냄 |
| 실루엣이 로봇으로 읽힘 | 네모난 어깨 | 둥근 덩어리 + 대각선 하나 |

### 지금까지 그린 것

주인공 셋(전사 · 궁수 · 도적), 적 넷(기는 것 · 뛰는 것 · 날것 · 단단한 놈),
탑 안쪽 벽과 발판 셋, 보스 다섯과 그 탄 다섯, 그리고 발판 위 아이템 열여덟 장
(진짜 아홉 · 가짜 여섯 · 폭탄 · 모션용 조각 둘). 아래 **우선순위의
1 · 2 · 3 · 4번과 7.5절**이 끝났고, **전부 게임에 붙어 있습니다.**

| 보는 곳 | 무엇 |
|---|---|
| `shots/art-preview.png` | 사람과 적을 3배로 키운 것과 실제 크기로 나란히 |
| `shots/art-scene.png` | 벽 위에 발판과 사람을 세운 장면. **벽 이음매가 한가운데** |
| `shots/art-boss.png` | 보스 다섯을 투기장 위에 띄우고 사람을 세워 크기를 잼 |
| `shots/art-items.png` | 아이템을 벽 위에 맨몸으로. **진짜와 가짜를 나란히** |
| `shots/art-compare.png` | 진짜 게임 안에서 지금 도형과 나란히 |
| `shots/items-ingame.png` | **발판 위에 실제로 놓인 아이템.** 진짜 · 가짜(드러나기 전) · 가짜(드러난 뒤) |

벽처럼 큰 것은 4배로 구우면 2000×3840 이 되어 무겁습니다. svg 뿌리에
`data-bake-scale="2"` 를 적으면 그 그림만 배율을 달리 굽습니다.

## 9. 다 그린 뒤

`assets/` 폴더를 만들어 위 이름 그대로 넣어 주세요.

```
assets/
  player-warrior.png
  e-crawler.png
  ...
```

크기는 **4배 그대로** 두셔도 됩니다 — 줄이는 것은 코드에서 하겠습니다.
넣어 주시면 제가 `js/textures.js`를 걷어내고 불러오도록 바꾸고,
크기·중심·충돌 범위를 지금 움직임에 맞게 다시 맞추겠습니다.

---

## 우선순위

한 번에 다 하실 필요 없습니다. 이 순서면 **적은 장수로 가장 크게 달라집니다.**

1. ~~**주인공 셋** — 늘 화면에 있습니다~~ ✔
2. ~~**적 넷** (기는 것 · 뛰는 것 · 날것 · 단단한 놈) — 100층까지 만나는 전부~~ ✔
3. ~~**탑 안쪽 벽과 발판** — 화면의 대부분을 차지합니다~~ ✔
4. ~~**보스** — 한 장인데 순간의 무게가 가장 큽니다~~ ✔ (다섯으로 늘렸습니다)
4.5 ~~**발판 위 아이템** (7.5절) — 아이템을 줄인 뒤로 한 칸의 무게가 커졌습니다~~ ✔
5. 나머지 적 여덟 ← 다음
6. 이펙트와 코인
7. 무기 서른여섯 (희귀도 색을 정한 뒤에)
